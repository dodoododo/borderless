// import { Router } from 'express';
// import { 
//   searchPlaces, 
//   matchPlace, 
//   askPlaces, 
//   getPlaceDetails, 
//   getPlacePhotos, 
//   getPlaceTips 
// } from '../controllers/place.controller.js'; 

// const router = Router();

// // 1. Các route tĩnh (phải đặt ở trên cùng)
// router.get('/search', searchPlaces);
// router.get('/match', matchPlace);
// router.get('/ask', askPlaces);

// // 2. Các route động chứa fsq_id (bắt buộc phải đặt ở dưới cùng)
// router.get('/:fsq_id/details', getPlaceDetails);
// router.get('/:fsq_id/photos', getPlacePhotos);
// router.get('/:fsq_id/tips', getPlaceTips);

// export default router;